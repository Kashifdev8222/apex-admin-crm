import { PageSkeleton } from "@/components/PageSkeleton";

/** Instant feedback while the next tab’s data loads (shell stays put). */
export default function OpsLoading() {
  return <PageSkeleton />;
}
